# Sample rules

These rules are small teaching examples for the CLI.

- `windows_failed_logon.yml` is a valid-looking learning rule.
- `broken_rule.yml` is intentionally incomplete, including an empty selector, so the checker has something to report.
